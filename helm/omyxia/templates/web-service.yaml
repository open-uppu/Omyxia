apiVersion: v1
kind: Service
metadata:
  name: {{ include "omyxia.fullname" . }}-web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: omyxia-web
