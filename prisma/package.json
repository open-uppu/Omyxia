{
  "name": "@omyxia/prisma",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:migrate": "prisma migrate deploy",
    "db:reset": "prisma migrate reset --force",
    "db:seed": "pnpm exec dotenv -e ../.env -- ts-node seed.ts",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.4.2",
    "prisma": "5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
